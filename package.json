{
  "name": "stat-tracker",
  "private": true,
  "dependencies": {
    "@hotwired/stimulus": "^3.0.1",
    "esbuild": "^0.14.18",
    "stimulus": "^1.1.1"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds"
  }
}
